# `swagger`

Swagger specs.

`application-platform.yaml` is a deterministic server-side copy of the SSOT
Application Platform OpenAPI contract. Run `make swagger.application-platform.sync`
to update it and `make swagger.application-platform.verify` to detect drift.
