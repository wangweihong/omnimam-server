
# generic — 泛型类型约束

**功能**：定义泛型类型约束，供其他包使用。

| 类型 | 说明 |
|---|---|
| `Number` | `int \| int32 \| uint32 \| int64 \| uint64 \| float32 \| float64` |
| `Int` | `int \| int8 \| int16 \| int32 \| int64 \| uint \| uint8 \| uint32 \| uint64` |
| `Ordered` | 支持 `<`, `<=`, `>`, `>=` 运算符的类型 |

[← 返回包列表](../../README.md)
